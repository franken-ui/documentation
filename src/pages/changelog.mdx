import DisplayAd from "layouts/ads/display-ad.astro";
import Mini from "layouts/mini.astro";

<Mini doc={{ title: "Changelog", meta: { robots: "noindex" } }} pathname="/changelog">

# Changelog

<p class="uk-text-lead uk-paragraph">Latest updates and announcements.</p>

<DisplayAd />

## v0.2.0 (August 19, 2024)

This release might be a breaking change to some. If you're using components as written on the docs, as it is, and did not make any customization, you should be fine and can skip this part.

### Possible breaking change

**1\.** Renamed `.uk-combobox` to `.uk-custom-select`.

The component previously named `.uk-combobox` has been renamed to `.uk-custom-select` for clarity. Additionally, `combobox` has been removed and all custom components are relocated to the new `extensions` component. So, if you're developing your own theme, customizing `<uk-select>`, using the fine-grained config, please remove `combobox` and transfer your customizations to the `extensions` component.

This change was made to simplify the process of adding new components (on my end), which currently requires editing 7 or 8 TypeScript files—an inefficient approach. Moving forward, all new components will be placed here, as they are not part of the UIkit components.

```js
components: {
    extensions: {
        hooks: {
          'hook-misc': {}
        }
    }
}
```

### What's new

We now have search, finally! You can also trigger it by pressing <kbd class="uk-kbd">⌘ /</kbd> (`CTRL + /`).

- Added the [Command](/docs/command) component.
- Introducing [Lucide](/docs/lucide) component. Note this is optional and can be used in or outside of Franken UI. It has a separate installation because of its size.
- The [Input Tag](/docs/input-tag) now receives focus when you're typing tags. Error and disabled states are also added.
- Fixed [case sensitivity](https://github.com/franken-ui/ui/issues/18) issue when searching in [Select](/docs/select).

And, that's it! You can now go back working on your amazing project. But, you can also keep reading if you want.

### Project and personal updates, transparency, etc

I want to share a little milestone: Franken UI is now over 4 months old, and I never expected it to gain this much traction. In this time, I've made significant changes and bug fixes, and it will keep getting better!

A big thank you to everyone who has shared the project, starred the repo, tweeted, donated, or filed issues. Your support means a lot to me.

For transparency, I signed up for AdSense about three weeks ago and was surprised to get approved within a day. I've placed banner ads below the documentation titles to help cover server expenses since I'm self-hosting. I've invested a lot of time and effort into this project, and I want to ensure it remains sustainable. The ad revenue is modest, generating between **$0.3** to **$0.6** per day, which is a welcome contribution to offsetting my server costs.

Many of you might not notice the ads, especially since over 70% of our traffic uses AdBlock, which is understandable—I'm a uBlock Origin fan myself! 🤫

Just to clarify, I will only run banner ads; any other ads you see are likely added by Google. I'm still learning and experimenting with this approach. I felt I had to resort to ads since I'm running on savings, the project is gaining traction, and I want to avoid selling premium components. Interest has really picked up, with traffic doubling in the past two weeks. However, I'm unsure about usage stats, as NPM data can be unreliable, and we also have CDN installations. My main goal is to keep this project alive.

Lastly, some personal updates: Last year, I stepped back from work to focus on my health, and it's been over a year now. My savings are projected to run out in about five or six months, so I'm starting to look for job opportunities. I was hired last June, but the role wasn't the right fit, so I didn't continue.

If you have any freelance projects or opportunities or a business looking to hire a developer, please reach out to me via [email](mailto:sveltecult@proton.me). I'm using every resource I have to promote myself—this is my website, after all. While it may seem like a team effort, I'm the only one behind the scenes! 😅

That's all for now and thank you for taking the time to read this.

## v0.1.3 (August 5, 2024)

The [Tag](/docs/input-tag) and [PIN](/docs/input-pin) inputs have been around since `v0.0.9`, but they never really had JavaScript behavior. Now that we have custom components, why not make them first-party components so you don't have to resort to external libraries? And, if you did, just note that their class names have changed for consistency. So, if you're using the following classes, here are the changes:

| **Original**        | **New**                   |
| ------------------- | ------------------------- |
| `.uk-tag-input`     | `.uk-input-tag`           |
| `.uk-pin-input`     | `.uk-input-pin`           |
| `.uk-pin-separated` | `.uk-input-pin-separated` |

Also, the class `.uk-tag` has had its background default background remove in favor of modifiers `.uk-tag-primary`, `.uk-tag-secondary` and `.uk-tag-danger`.

### Other changes

- Better event dispatching on the [Select](/docs/select) component.
- Revival of [Templates](/docs/introduction#installation). Right now, we only have [Laravel](https://github.com/franken-ui/template-laravel), [Astro](https://github.com/franken-ui/template-astro) and [HTML](https://github.com/franken-ui/template-html). These are very useful, so we can all start coding right away. If you maintain templates for your language and want to share them with the community, please feel free to share them (you can open an issue on Github or start a discussion or whatever) and I'd be happy to link them here in the docs.

## v0.1.2 (August 2, 2024)

A lesson learned for all of us using [Hooks](/docs/hooks).

As you probably know, our current "skin" (shadcn/ui) is essentially just a bunch of pre-configured hooks. If you're not familiar with Hooks, it's a way of saying, "Hey, override these rules."

### Who's affected

People who are using `v0.1.1` for the plugin and `v0.0.4` for CDN.

### Why the code was bad

![Bad Hook](/images/screenshots/Screenshot%202024-08-02%20141432.png)

So the code above, essentially means, Find me the key of `@media (min-width: 768px)` and replace it with:

```javascript
.uk-comment-list .uk-comment ~ ul: {
    paddingLeft: null
}
```

What it means is, I want to remove the `paddingLeft` created by UIkit. Although I'm forgetting one thing: media queries won't nest the rules.

So, what happened was it replaced all rules under `@media (min-width: 768px)` with nothing. Because when you put `null` as value, we're essentially deleting that rule.

### The proper way

The proper way of removing the `paddingLeft` was just to target the rule, and call Tailwind CSS utility from there. So, in our case, there is a hook available called `hook-list-sub` and it will target `.uk-comment-list .uk-comment ~ ul`. So, it should have been:

```javascript
'hook-list-sub': {
    '@apply ml-6 mt-6 border-l border-border md:pl-0': {}
}
```

Where we simply need to put `md:pl-0`.

### Lesson learned

Do not mess with media queries. Instead, call responsive utilities, such as `sm:*`, `md:*`, `lg:*`, etc and let the Tailwind CSS or PostCSS do the rest.

## v0.1.1 (July 26, 2024)

A couple of minor additions and fixes for this month.

- Introducing [Glow](/docs/glow) component.
- Better keyboard navigation for [Select](/docs/select) component.
- Add `i18n` attribute to [Select](/docs/select#internationalization) component.
- Revamped [Comment](/docs/comment) component to better adhere to shadcn/ui design.
- Increased the default Accordion padding to `1rem`.
- Fixed the [missing blue theme](https://github.com/franken-ui/ui/issues/13) on CDN installation.

## v0.1.0 (July 14, 2024)

### DX-Focused Release

This version is all about reducing friction and helping you get started with your app as quickly as possible.

### Breaking changes

**1\.** Safelist Property Removal

The `safelist` property have been removed from `presetQuick`. If you're using multiple presets, Tailwind CSS might ignore this property. To avoid issues, move the `safelist` to your own `tailwind.config.js` file.

```diff
 /** @type {import('tailwindcss').Config} */
 export default {
+  safelist: [
+    {
+      pattern: /^uk-/,
+    },
+  ],
 };
```

That's it! The rest of the changes are optional, and you can continue with your existing setup. However, if you want to take advantage of the DX improvements, keep reading.

### Optional changes

**1\.** Simplify UIkit Imports

If you're importing UIkit and its icons from NPM using the `uikit` package, you can remove that import. Franken UI now includes UIkit as a `peerDependency`. Instead, import UIkit from `franken-ui` in your `app.js` file.

```sh
npm remove uikit
```

```diff
- import UIkit from "uikit";
- import Icons from "uikit/dist/js/uikit-icons.js";
+ import { UIkit, Icons } from "franken-ui/uikit/js/dist";

 UIkit.use(Icons);

 window.UIkit = UIkit;
```

**2\.** Streamline PostCSS Config

If you have `postcss-sort-media-queries` and `postcss-combine-duplicated-selectors` installed, you can remove them in your `postcss.config.js` file and import from `franken-ui` instead.

```sh
npm remove postcss-sort-media-queries postcss-combine-duplicated-selectors
```

```diff
 module.exports = {
      plugins: [
         require('tailwindcss'),
-        require('postcss-sort-media-queries')({
+ 		   require('franken-ui/postcss/sort-media-queries')({
             sort: 'mobile-first'
         }),
-        require('postcss-combine-duplicated-selectors')({
+		     require('franken-ui/postcss/combine-duplicated-selectors')({
             removeDuplicatedProperties: true
         })
      ]
 };
```

These changes are optional, and you can revert to your previous setup if you encounter any issues.

### What's new

- Introducing the `npx franken-ui init` command! Learn more about it on our [new installation](/docs/installation#installation-via-npm) page.
- Added `only` and `except` properties for `presetQuick`. Learn more about these on our [new installation](/docs/installation#picking-components) page.
- Added `small` and `xsmall` sizes for button, icon and iconnav components, and `small` variant for form input. Also, added `shadow-sm` for icon component.
- Added the highly requested [combobox](/docs/select#combobox) and [custom select](/docs/select) components.

## v0.0.13 (June 8, 2024)

- Add support for [theme generators](/docs/theming#using-theme-generators) or watch the demo on [Mastodon](https://mas.to/@sveltecult/112556172122401221) on how to configure your own palette.
- Add [Stepper](/docs/stepper) component.
- Fix `.uk-list` color modifiers.
- Internal changes such as exposing the entire `shadcn-ui` path instead of listing subpaths, `hexes.js` now exports function to convert HSL to hex instead of hardcoded hex.
- Upgrade UIkit from `3.20.8` to `3.21.5`.

## v0.0.12 (May 20, 2024)

- Add [Typography](/docs/typography) classes
- Fix `uk-divider` height
- Fix `uk-description-list-divider` wrong border color and hook
- Remove dead classes in card component
- Refactor docs to use MDX for better maintenance, syntax highlighting, formatting and copying and pasting
- Refactor docs to have better examples that works for both as a standalone or as a Tailwind CSS plugin
- Fix modal width
- Fix missing hooks and components in `presetQuick()`
- Change default navbar gap to `gap-6`

## v0.0.11 (May 11, 2024)

- Introducing the [CDN installation](https://github.com/franken-ui/wc).
- Fix missing hooks and components in `presetQuick()`
- Add sensible padding for modal, table, tab components to make way for CDN aka "no-build" installations. If you are having trouble overriding some paddings, you can always use the not important utilities `!p-6` or consider using [hooks](/docs/hooks).
- Change buttons' shadow from `shadow` to `shadow-sm`
- Add rounded corners on modal even on xs devices
- Fix notification alignment on xs devices
- Change UIkit breakpoints to match Tailwind CSS. Here are the list of changes:

| **Screen** | **Original** | **New** |
| ---------- | ------------ | ------- |
| SM         | 640px        | 640px   |
| MD         | 960px        | 768px   |
| LG         | 1200px       | 1024px  |
| XL         | 1600px       | 1280px  |

## v0.0.10 (May 1, 2024)

- This release focus mainly on ease of use, code cleanup and introduction of [`presetQuick()`](/docs/installation)
- Fix `uk-accordion-title` alignment
- Fix `uk-tab-left` and `uk-tab-right` wrong border color
- Refactor `hooks.ts` for better maintenance
- Demystifying [hooks](/docs/hooks)

## v0.0.9 (April 30, 2024)

- Add tag and PIN inputs
- Increase default padding for `.uk-nav-primary` and `.uk-nav-secondary`
- Fix height and line-height of `.uk-button-text` and `.uk-button-link`

</Mini>
